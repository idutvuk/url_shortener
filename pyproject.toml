[project]
name = "url_shortener"
version = "0.1.0"
description = "Dead simple url shortener for me"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.9",
    "sqlmodel>=0.0.27",
    "uvicorn>=0.34.0",
]
